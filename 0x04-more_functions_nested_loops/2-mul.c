#include "main.h"
/**
 * mul - multiplies two integers
 * @a: int for multiply
 * @b: int for multiply
 * @res: multiplication result
 * return: result
 */
int mul(int a, int b)
{
int res;
res = (a * b);
return (res);
}
