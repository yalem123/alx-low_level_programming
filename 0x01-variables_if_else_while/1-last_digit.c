#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 * Return: 0 Always 
 */
int main(void)
{
int n,lastd;

srand(time(0));
n = rand() - RAND_MAX / 2;
printf("Last digit of %d is %d )

	return (0);
}

