#include "main.h"
/**
 * _strlen - function that returns the length of a string
 * @s: string
 * Return: 0 Always
 */
int _strlen(char *s)
{
char su[] = "yalem";
_putchar("Length of string is: %d", su[]);
return (0);
}
